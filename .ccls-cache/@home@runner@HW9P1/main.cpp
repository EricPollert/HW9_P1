#include <iostream>
#include <iomanip>

using namespace std;

int main() {
  
  const int COL_NUM = 7;
  const int ROW_NUM = 60;
  int grade [ROW_NUM][COL_NUM] = {{}

  for (int r = 0; r < NUM_ROWS; r++){
  for (int c = 0; c < NUM_COLS; c++){
    cout << setw(5) << myMat[r][c];
  
    
    
    }
    cout << endl;
  }
  
}